@inherits Umbraco.Cms.Web.Common.Views.UmbracoViewPage<ContentModels.HomePage>
@using ContentModels = Umbraco.Cms.Web.Common.PublishedModels;
@using Umbraco.Cms.Core.Models.PublishedContent;
@{
    Layout = "Master.cshtml";
}

<main class="container my-4">
    <h3>@Model.PageTitle<i class="fas fa-star"></i></h3>

        <p>@Model.BodyText</p>
        
                @{
            var carouselSlides = new List<(string ImageUrl, string Description)>{
        ("/images/1.png", ""),
        ("/images/2.png", ""),
        ("images/3.png", ""),
        };
        }

        @if (carouselSlides.Any())
        {
            <div id="carouselExampleIndicators" class="carousel slide m-4" data-ride="carousel">
                <ol class="carousel-indicators">
                    @for (var i = 0; i < carouselSlides.Count; i++)
                    {
                        <li data-target="#carouselExampleIndicators" data-slide-to="@i" class="@(i == 0 ? "active" : "")"></li>
                    }
                </ol>
                <div class="carousel-inner">
                    @foreach (var (imageUrl, description) in carouselSlides)
                    {
                        var carouselIndex = carouselSlides.IndexOf((imageUrl, description));
                        <div class="carousel-item @(carouselIndex == 0 ? "active" : "")">
                            <img src="@imageUrl" class="d-block w-100 carousel-image">
                            <div class="carousel-caption d-none d-md-block"><p>@description</p></div>
                        </div>
                    }
                </div>
                <a class="carousel-control-prev" href="#carouselExampleIndicators" role="button" data-slide="prev">
                    <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                    <span class="sr-only">Previous</span>
                </a>
                <a class="carousel-control-next" href="#carouselExampleIndicators" role="button" data-slide="next">
                    <span class="carousel-control-next-icon" aria-hidden="true"></span>
                    <span class="sr-only">Next</span>
                </a>
            </div>
        }
        
@section NotificationBanner
{
    @if(Model.ShowNotificationBanner){
        <div class="homePageBanner" style="text-align: center; width: 100%; padding: 20px 20px; background-color: #fad634;">
            <h3 style="font-weight:700;">@Model.BannerTitle</h3>
            <p>@Model.TextContent</p>
        </div>
    }

	

}


</main>

<script src="https://code.jquery.com/jquery-3.6.0.min.js" integrity="sha384-KyZXEAg3QhqLMpG8r+Knujsl5/J6mzivaso2SUMxyt8jGlayoutsNi0<uV00AYrió/>" crossorigin="anonymous"></script>
<script src="https://cdn.jsdelivr.net/npm/@@popperjs/core@2.11.6/dist/umd/popper.min.js"></script>
<script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
<script src="/scripts/main.js"></script>



